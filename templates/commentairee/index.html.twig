{% extends 'base.html.twig' %}

{% block title %}Commentairee index{% endblock %}

{% block body %}
    <h1>Commentairee index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdC</th>
                <th>Message</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commentairee in commentairees %}
            <tr>
                <td>{{ commentairee.idC }}</td>
                <td>{{ commentairee.message }}</td>
                <td>
                    <a href="{{ path('commentairee_show', {'idC': commentairee.idC}) }}">show</a>
                    <a href="{{ path('commentairee_edit', {'idC': commentairee.idC}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('commentairee_new') }}">Create new</a>
{% endblock %}
