<form method="post" action="{{ path('commentairee_delete', {'idC': commentairee.idC}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentairee.idC) }}">
    <button class="btn">Delete</button>
</form>
